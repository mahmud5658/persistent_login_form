name: persistent_login_form
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  flutter_svg: ^2.1.0
  google_fonts: ^6.2.1
  flutter_screenutil: ^5.9.3
  device_preview: ^1.2.0
  get: ^4.7.2
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/

